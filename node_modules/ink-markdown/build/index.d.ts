/// <reference types="react" />
import { TerminalRendererOptions } from 'marked-terminal';
declare type Props = TerminalRendererOptions & {
    children: string;
};
export default function Markdown({ children, ...options }: Props): JSX.Element;
export {};
